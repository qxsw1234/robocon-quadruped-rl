# DIY 导航机器人项目执行计划（按里程碑批量推进）

## 已核实现状（探索代理勘察结论）
- `~/ros2_ws` 为 git 仓库（5 提交，干净）；4 包齐全：`diy_nav_description`（阶段 4 已完成）、`diy_nav_gazebo`/`diy_nav_navigation`（全 `.gitkeep` 占位）、`diy_nav_slam_compare`（仅 CLI 骨架）
- 环境已装齐：Ubuntu 22.04.4 + Humble + Gazebo Classic 11.10.2 + Nav2 1.1.20 + slam_toolbox 2.6.10 + cartographer 2.0.9；`.bashrc` 缺 `~/ros2_ws/install/setup.bash` source 与 GAZEBO_MODEL_PATH
- 计划中一处数值偏差已确认：laser_link 实际离地 0.315 m（非计划假设的 0.265），需同时收薄上盖才能落到规格 0.23 m

## 批次 1：仿真打通（阶段 4.5 + 5 + 6 + 7）

### 阶段 4.5 前置修复（先做，改前必读文件）
1. `urdf/properties.xacro`：`base_length` 0.46→0.45、`base_width` 0.34→0.32、`wheel_separation` 0.31→0.36（修轮子埋入底盘）、`caster_x_offset` −0.16→+0.16、`upper_height` 0.08→0.02、`laser_mount_height` 0.035→0.01（激光中心精确落 0.23 m，注释同步修正）
2. `urdf/wheels.xacro`：`rear_caster_link/joint` → `front_caster_link/front_caster_joint`（规格 TF 树），核对 caster 球底离地 0 悬空
3. 新建 `urdf/gazebo_plugins.xacro`（空壳 + 接口注释，先不含被禁 token），并在 `diy_nav_bot.urdf.xacro` 中 include
4. `scripts/validate_description.sh`：加 `--stage N` 参数（N≥5 解除 Gazebo 插件 token 禁令；slam_toolbox/cartographer 永远禁），REQUIRED_LINKS/JOINTS 同步改为 front_caster，修 map/odom joint 检查漏洞
5. 修正 `src/README.md` 陈旧状态表 + 两处 README 的尺寸/TF 树
6. `~/.bashrc`：先备份 → 给 diff → 经您确认后幂等追加（source setup.bash + GAZEBO_MODEL_PATH）
7. 验证：xacro → check_urdf → colcon build → validate `--stage 4` → `display.launch.py` 目视（轮子外露、caster 贴地、雷达在顶部）
8. 提交 `fix: align robot dimensions with spec and add gazebo plugin interface`

### 阶段 5 Gazebo 接入（Classic 11）
- 实现 `gazebo_plugins.xacro`：差速驱动（`wheel_separation=0.36`、`publish_odom_tf=true`、`publish_wheel_tf=false`、30 Hz）+ 关节状态 + 2D 激光（720 samples、−π~π、10 Hz、0.12–8.0 m、σ=0.01 → `/scan`）+ IMU（50 Hz → `/imu`）+ 轮摩擦（mu1=mu2=1.0）/caster 零摩擦 + 6 色材质映射
- `worlds/empty_world.world`（内联 ground_plane + 方向光，零 Fuel 依赖）、`launch/simulation.launch.py`（world/x/y/z/yaw/rviz/headless/use_sim_time）、`config/gazebo_params.yaml`、`rviz/simulation.rviz`
- `scripts/test_motion.sh` 自动验证：前进/原地转/停止，读 `/odom` 校验，PASS/FAIL
- 验收：/clock /cmd_vel /odom /tf /joint_states /scan /imu 全有数据、odom→base_footprint 唯一、能前进/原地转/停住不漂移；您目视 Gazebo 画面确认
- 提交 `feat: integrate DIY robot with Gazebo Classic 11 simulation`

### 阶段 6 两个自建世界
- `simple_world.world`（10×8 m：墙+中央障碍+2 箱+直角弯+通道≥1.0 m+起点区+3 目标区）、`complex_world.world`（16×12 m：多房间+长走廊+窄通道 0.70–0.85 m+U/L 形障碍+回环+遮挡区+≥5 目标区）
- 纯 SDF 基本几何体、6 组自建材质、目标点薄圆柱无 collision；`world:=simple|complex|empty` 简写；`diy_nav_gazebo/README.md` 附初始位姿/目标点坐标表
- 提交 `feat: add custom simple and complex Gazebo worlds`

### 阶段 7 接口与 TF 验收
- `scripts/validate_simulation.sh`（9 topic 存在性+类型+发布订阅+频率+TF 连通+重复 TF 检测）、`scripts/teleop_test_suite.sh`（直行 3 m/后退 1 m/左右 360°/绕障/停止漂移/穿模检测，从 /odom+/scan 判定）
- `docs/robot_interfaces.md`、`docs/tf_tree.md`、`docs/topic_list.md`（七字段表）
- 提交 `test: add simulation interface and TF validation`

**批次 1 结束：8 段汇报 + 目视验证确认后进入批次 2**

## 批次 2：建图与导航（阶段 8 + 9 + 10）
- **阶段 8 SLAM Toolbox**：以官方 `mapper_params_online_async.yaml` 为基线写 `slam_toolbox.yaml`（16 关键参数显式化+中文注释）；`slam_toolbox.launch.py` + `slam.rviz`；`scripts/mapping_tour.py` 按 complex_world 航点自动建图（外圈→房间→窄通道→闭环），`map_saver_cli` 存图；`docs/slam_toolbox_notes.md`；提交 `feat: add SLAM Toolbox mapping pipeline`
- **阶段 9 AMCL + Nav2**：以 nav2_bringup 官方 params 为基线改 `nav2_params.yaml`（footprint `[[0.245,0.175],...]` 不设 robot_radius、节点名按 Humble 1.1.20 实装核对）；`localization.launch.py` + `navigation.launch.py` + `navigation.rviz`；`scripts/nav_goal_runner.py`（nav2_simple_commander，**先发 /initialpose 再等 lifecycle active** 规避已知风险，5 目标顺序测，记录到 CSV）；提交 `feat: add AMCL localization and Navigation2 autonomous navigation`
- **阶段 10 调优**：先备份参数，按 7 组逐组调（运动限制/footprint/障碍层/膨胀层/规划器/控制器/行为树），每组测完记 `docs/nav2_tuning_log.md`（七字段）；3 轮 × ≥5 目标成功率 ≥80%；补 `docs/nav2_nodes.md`/`nav2_topics.md`/`nav2_parameters.md`/`nav2_tf_explanation.md`；提交 `tune: optimize Navigation2 parameters for custom robot`

**批次 2 结束：汇报成功率实测数据 + 确认后进入批次 3**

## 批次 3：bringup、对比实验与交付（阶段 11 + 12 + 13 + 14）
- **阶段 11 一键 Bringup**：`bringup.launch.py`（14 参数，RegisterEventHandler/OnProcessStart 解依赖，不用长 Timer；关停清理 gzclient/container）；`smoke_test_bringup.sh`；冷启动测试；更新根 README；提交 `feat: add unified SLAM and navigation bringup`
- **阶段 12 Cartographer**：官方 Lua 基线改 6 项（use_imu_data=false 与 slam_toolbox 公平比较）；互斥保护（检测 slam_toolbox 在跑则退出）；复用 `mapping_tour.py` 同路线建图存图；提交 `feat: add Cartographer 2D SLAM comparison pipeline`
- **阶段 13 公平对比**：录一次 bag 离线分别回放喂两算法（/scan /odom /tf 完全一致）；4 个 Python 工具（resource_monitor/map_statistics/navigation_result_logger/generate_report）+ 8 项指标 + `results/时间戳/` 不覆盖旧实验；30 次导航试验（两地图 × 5 目标 × 3 次）；结论由实测数据得出；提交 `experiment: add reproducible SLAM comparison workflow`
- **阶段 14 最终交付**：`docs/` 15 份（system_architecture.md 含 Mermaid；troubleshooting.md 只写实际错误六段式；demo_script 15 节；defense_questions ≥30 问如实回答 Humble+Classic 决策）；最终 `rm -rf build install log` 全量重建 + colcon test + 冷启动 `bringup mode:=navigation world:=complex` + `FINAL_CHECKLIST.md`；提交 `docs: finalize autonomous navigation project delivery`

## 执行约定
- 全程用 TodoWrite 跟踪；每阶段：改前先读文件、不删已有代码、根因修复不注释规避；每阶段按计划文档的提交信息单独 git commit
- 需要 sudo / 改 ~/.bashrc 前先给 diff 等确认；grep 用 `command grep`（zcode 环境包装）；GUI（RViz/Gazebo）目视节点在汇报中标明
- 批内阶段连续执行，批末 8 段格式汇报（本批完成内容/新建修改文件/执行命令/构建结果/功能测试结果/仍存问题/需您手动验证项/下批建议）并暂停等确认