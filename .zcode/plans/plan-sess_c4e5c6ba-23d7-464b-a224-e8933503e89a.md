# 收尾实施计划

## 当前状态判定
- **代码 100% 完成**：22 个规范文件齐全，`colcon build` 通过，`pytest` 31/31 全过。
- **文档 ≥95%**：20 章全在，22 类故障排查全覆盖（只是挂在独立 `## 故障排查` 章节下，而不是直接放在「第 18 章 常见错误」标题里）。
- **实机验证 0%**：`config/camera.yaml` 是零占位；`results/` 全空。你已确认「暂不做实机实验」，且棋盘格/AprilTag 都还没打印，所以本轮**不涉及**任何实机流程。

## 本轮要做的收尾工作（3 步，纯文档 + 一次防御性重跑）

### 步骤 1 — README 第 18 章结构重构（对应你的选择"搬过来"）
文件：`~/apriltag_pose_ws/src/k230_apriltag_pose/README.md`

改动方式：
1. 把当前「`## 故障排查`」章节下的 22 条内容整块搬到「`## 18. 常见错误`」标题之下，作为该章的正文。
2. 删除原「`## 故障排查`」独立章节（避免重复）。
3. 保持 22 条内容一字不改；只调整位置。
4. 更新目录/内部引用（如 §18 前后指针）。

预计 diff：README.md 中 ~30 行位置调整，无新增排查条目。

### 步骤 2 — 追加「当前实机验证状态」小节
在 README 顶部「项目功能」章节后（或 §20 之后）新增一个明确的状态小节，用简短表格说明：

| 项目 | 状态 |
|---|---|
| 代码 / 编译 / 单元测试 | ✅ 完成（31/31） |
| 棋盘格标定板 | ⏳ 待打印+测量+采集 |
| AprilTag（tag36h11 ID 0） | ⏳ 待打印+测量 |
| `config/camera.yaml`（内参） | ⏳ 当前是零占位，待标定生成 |
| K230 UVC | ⏳ CanMV 固件 = CDC+MTP，无 /dev/videoX；已按规范情况 B 使用电脑内置摄像头方案 |
| 距离误差实验 4×2×20 帧 | ⏳ 待实机采集 |
| `results/` 产物 | ⏳ 待实机运行后生成 |

目的：让考核方一眼看到「哪些是代码交付」「哪些是待实机部分」，避免把零占位标定当成结果。

### 步骤 3 — 防御性重跑构建与单测
上一次成功构建在 15:45，之后没有源码改动（只改 README）。为保险仍执行：
```bash
cd ~/apriltag_pose_ws
colcon build --symlink-install --packages-select k230_apriltag_pose
colcon test --packages-select k230_apriltag_pose
colcon test-result --verbose
```
期望：31 tests passed，0 failures。

## 明确不做的事
- ❌ 不打印/不引导打印棋盘格、AprilTag（本轮不涉及物料）。
- ❌ 不跑摄像头标定、不覆盖 `config/camera.yaml`。
- ❌ 不采集距离数据，不生成 `distance_measurements.csv` / 汇总 / 图 / 报告。
- ❌ 不产 TF 树 PDF、不产 RViz2 / rqt_image_view 截图。
- ❌ 不刷 K230 固件。
- ❌ 不动 `~/apriltag_pose_ws/src/apriltag_camera_pose/`（旧包保留）。
- ❌ 不动 `~/ros2_ws`（用户导航项目）。

## 交付
- 更新后的 `README.md`（§18 内容化 + 状态表）。
- 一次成功的 `colcon build` + `colcon test`（31/31 pass）日志摘要。
- 按规范格式的阶段报告（阶段名 / 执行命令 / 检查结果 / 生成文件 / 是否通过 / 存在的问题 / 下一步）。